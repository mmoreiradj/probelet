apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: probes.probelet.dev
spec:
  group: probelet.dev
  names:
    categories: []
    kind: Probe
    plural: probes
    shortNames:
    - probe
    singular: probe
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v0
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ProbeSpec via `CustomResource`
        properties:
          spec:
            description: Generate the Kubernetes wrapper struct `Probe` from our Spec and Status struct
            properties:
              kind:
                description: The kind of probe to use
                oneOf:
                - required:
                  - Http
                properties:
                  Http:
                    description: A HTTP probe
                    properties:
                      method:
                        description: The HTTP method to use
                        type: string
                      url:
                        description: The URL to monitor
                        type: string
                    required:
                    - method
                    - url
                    type: object
                type: object
            required:
            - kind
            type: object
          status:
            description: The status object of `Probe`
            nullable: true
            type: object
        required:
        - spec
        title: Probe
        type: object
    served: true
    storage: true
    subresources:
      status: {}
